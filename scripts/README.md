# Scripts

## validate_sample_data.py
Validates that files in `sample-data/`:
- are valid JSON
- have required top-level fields for known scaffold samples

### Usage
From repository root:

```bash
python scripts/validate_sample_data.py
```
